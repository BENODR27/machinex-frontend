import { UtcToLocalPipePipe } from './utc-to-local.pipe-pipe';

describe('UtcToLocalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UtcToLocalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
